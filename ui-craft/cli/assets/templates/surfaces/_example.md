# {Surface Name}

<!-- Lazy-loaded — loaded only when the current task references this surface by name.
     Rename this file to the surface name (e.g. dashboard.md, settings.md, onboarding.md).
     One file per major surface. Do not create surface files eagerly — add them as surfaces mature. -->

## Layout

<!-- Grid structure, breakpoints, column layout, sticky elements, scroll behavior.
     Example: "Two-column: 240px fixed sidebar + fluid main. Mobile: sidebar collapses to bottom nav." -->

## Components

<!-- Which components appear on this surface and any surface-specific variants.
     Example:
     - DataTable with row actions (edit/delete inline)
     - MetricCard (no sparkline on this surface — too dense)
     - EmptyState with illustration (first-time UX only) -->

## Notes

<!-- Edge cases, open questions, interaction details specific to this surface.
     Example: "The filter panel is lazy-loaded on first open to reduce initial bundle." -->
